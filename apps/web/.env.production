# ---------- Runtime ----------
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
TZ=Asia/Karachi

# ---------- Public URLs ----------
# Canonical site URL (SEO, absolute links, next-sitemap)
NEXT_PUBLIC_SITE_URL=https://filespay.org

# Your EXISTING backend base URL (absolute, NO trailing slash).
# Example: https://api.filespay.org or https://cloudways.yourdomain.com/web-api
NEXT_PUBLIC_API_BASE=https://<YOUR-BACKEND-BASE>

# ---------- Admin / UI ----------
# Keep 0 in production (do not force-show admin UI without auth)
NEXT_PUBLIC_FORCE_ADMIN=0

# ---------- Safety switch ----------
# Disable any local Next /web-api handlers in this app (frontend-only on Vercel)
DISABLE_LOCAL_API=1

# ---------- Optional ----------
# TinyMCE key (editor works without in dev, add if you have one)
NEXT_PUBLIC_TINYMCE_KEY=
