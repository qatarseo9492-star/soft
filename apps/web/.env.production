# ---------- Runtime ----------
NODE_ENV=production
PORT=3000
HOST=0.0.0.0
TZ=Asia/Karachi
NEXT_TELEMETRY_DISABLED=1

# ---------- Core service URLs ----------
# Web server talks to the API via this private base (change port here only if API moves)
API_BASE_SERVER=http://127.0.0.1:3011
# Browser hits only this relative path; never expose internal ports
NEXT_PUBLIC_API_BASE=/web-api
# Canonical site URL (used in JSON-LD, links, etc.)
NEXT_PUBLIC_SITE_URL=https://filespay.org

# Used by /api/revalidate (must match apps/api/.env)
WEB_REVALIDATE_TOKEN=REDACTED_CHANGE_ME

# ---------- Database (NOT used by web app now) ----------
# Leave these blank to avoid accidental Prisma usage or secret leaks from the web app.
WEB_DATABASE_URL=
DATABASE_URL=

# ---------- Meilisearch (optional) ----------
MEILI_HOST=http://127.0.0.1:7700
MEILI_KEY=
MEILI_INDEX=software

# ---------- Feature flags ----------
# 0 = require proper auth when you add it; 1 = force-enable admin UI (dev only)
NEXT_PUBLIC_FORCE_ADMIN=0
