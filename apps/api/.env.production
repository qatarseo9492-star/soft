# ---------- Runtime ----------
APP_NAME=filespay-web
NODE_ENV=production
HOST=0.0.0.0
PORT=3000
TZ=Asia/Karachi
NEXT_TELEMETRY_DISABLED=1

# ---------- Core service URLs ----------
# If your Next server (this app) needs to call your separate API service (Nest on :3011)
API_BASE_SERVER=http://127.0.0.1:3011

# Public base the BROWSER should call.
# Leave EMPTY to use this app’s own internal routes like /web-api/* (prevents /web-api/web-api duplication).
NEXT_PUBLIC_API_BASE=

# Canonical site URL (SEO, links)
NEXT_PUBLIC_SITE_URL=https://filespay.org

# Token used by /api/revalidate (MUST match your API’s WEB_REVALIDATE_TOKEN)
WEB_REVALIDATE_TOKEN=a53fc619aca9dbecf92594ce18a32b511e9d4e636b30a15c1557baaddad03df4

# ---------- Server-only Admin Auth for /admin & /web-api/admin/* ----------
# Choose strong values in production
ADMIN_USER=admin
ADMIN_PASS=change-me
ADMIN_JWT_SECRET=very-long-random-string-change-me-now

# ---------- Database (Prisma for /web-api routes) ----------
DATABASE_URL=mysql://kpvbrsfqas:ezMWSyBXV8@127.0.0.1:3306/kpvbrsfqas?connection_limit=5&pool_timeout=30

# ---------- Meilisearch (optional; leave empty if not running) ----------
MEILI_HOST=http://127.0.0.1:7700
MEILI_KEY=
MEILI_INDEX=software

# ---------- Feature flags ----------
# Set to 1 ONLY for dev (forces admin UI without login). Keep 0 in prod.
NEXT_PUBLIC_FORCE_ADMIN=0
